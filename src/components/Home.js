import React, { useEffect } from 'react';
import InitQuery from './InitQuery';

const Home = ({searchInput}) => {
  return(
    <div className="home">
      <InitQuery/>
    </div>
  )
}
export default Home